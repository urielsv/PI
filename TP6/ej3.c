/*
 * Dado un arreglo lineal de números reales, cuyo indicador de fin de elementos es 
 * cero, escribir una función para obtener la mayor diferencia entre dos elementos 
 * consecutivos. (En no más de 5 líneas). Tener en cuenta que los números pueden 
 * ser negativos. En caso de que el vector tenga un solo elemento deberá retornar 
 * como diferencia el valor cero.
 *
 * A partir de ahora, cuando escriban sus propias funciones y consideren que los 
 * arreglos que recibas como parámetros de entrada no deben ser alterados en el 
 * cuerpo de la función, no se olviden de colocar el calificador const.
 */
#include <stdio.h>

// koehler
int main() 
{
    char * a = NULL;
    printf("%d", a);
       
    return 0;
}
