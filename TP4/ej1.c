/*
 * Decidir, en caso de ser posible, con qué valor termina cada 
 * una de las variables en los siguientes programas. En caso de 
 * haber errores de compilación, corregirlos.
 * 
 * A: b = 3
 * B: Error de compilacion float a = 0;
 *      Error de compilacion: lvalue (PI CTE, PI++ <=> PI = PI + 1 lo cual es un
 *      lvalue error.
 * C: b = a + 1 * a + 1 * a + 1 = 13
 *    c = (a + 1) * (a +1) * (a + 1) = 25 
 */
